1 para instalar

	npm install -g node-sass

2 usar

	node-sass -w scss -o css



sass puto


1 instaar sass con npm

2 npm init

3 sass css:css  

4 sass --watch css:css (mira cualquier archivo scss en la caprta css : transformelos en css)

5 script 

	  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch scss:css"
  },